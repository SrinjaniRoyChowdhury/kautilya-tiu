export type Collaborator = {
  name: string;
  logoUrl?: string;
  websiteUrl?: string;
};

/** Update this list as collaborator logos and links are confirmed. */
export const COLLABORATORS: Collaborator[] = [];
