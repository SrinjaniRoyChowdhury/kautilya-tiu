import { COMMITTEE_LOGO_PX } from "@/lib/upload";

export { COMMITTEE_LOGO_PX };

export const COMMITTEE_LOGO_HINT = `Optional. Square ${COMMITTEE_LOGO_PX}×${COMMITTEE_LOGO_PX} px (1:1). JPEG, PNG, or WebP. You can add or change this later.`;
