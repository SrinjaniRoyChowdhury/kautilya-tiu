{
  "build": {
    "env": {
      "VERCEL_ENV": "preview"
    }
  },
  "ignoreCommand": "if [ \"${VERCEL_GIT_COMMIT_REF:-$VERCEL_GIT_BRANCH}\" != \"main\" ] && [ \"${VERCEL_GIT_COMMIT_REF:-$VERCEL_GIT_BRANCH}\" != \"staging\" ]; then exit 0; else exit 1; fi"
}
